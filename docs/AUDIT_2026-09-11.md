# 改善余地オーディット — 2026-09-11

> 版A（ルート直下ビルド）を対象に、コード/アセット/UX/アクセシビリティ/パフォーマンス/運用を全方位で棚卸しした結果です。  
> 前回の「アニメーション演出ブラッシュアップ（POLISH OVERHAUL）」は反映済み（`vn.css`1155行 / `visual.js`粒子深度 / `shell.js`スタッガー）。  
> ここでは**それ以外にまだ残っている改善余地**を、優先度付きで一覧化します。

---

## サマリ — 3行で

- **重大な不具合はなし**（`vncheck`全69シーン/14END到達、`smoke`全69シーン/14エンド/8種端末/save・load合格、構文ok）。土台は健全。
- **UXの完成度を決めるのは「視覚以外の4点」**：①アクセシビリティ ②パフォーマンス/配信 ③セーブ互換/エラーハンドリング ④PWA/OGP等の周辺。
- **すぐ効く Quick Win は10件**、腰を据える中長期は6テーマ。順に潰せば「素材差替えだけで商業同等」まで行ける。

---

## 判定スケール

- **P0**: 体験を壊す/壊しうる。次のPRで直すべき
- **P1**: 完成度を1段階上げる。2〜3PRで回収推奨
- **P2**: 磨き込み/将来投資。余裕があれば

見積は **S <2h / M 半日 / L 1日+**

---

## 1) アクセシビリティ（最大の伸びしろ）

| # | 項目 | 現状 | リスク | 提案 | 優先×工数 |
|---|---|---|---|---|---|
| A-1 | オーバーレイのフォーカストラップ/ `role="dialog"` / `aria-modal` / 背景の `aria-hidden` | `#overlay` は `hidden` トグルだけで、Tabで背面にフォーカスが抜ける。Escは効くがフォーカス復帰なし | キーボードユーザは閉じた後に迷子。スクリーンリーダは背景を読み上げてしまう | `open(kind)` で `trapFocus(ovPanel)`、`close()` でトリガー要素へ `focus()` 復帰。`ovPanel` に `role=dialog aria-modal aria-labelledby=ovTitle`、背景 `inert` | **P0 S** |
| A-2 | `prefers-reduced-motion: reduce` が canvas 粒子と WebAudio の LFO を止めていない | CSSは停止するが `Particles.tick()` / `audio._ambience()` は動き続ける | 酔い/バッテリー。OS設定を尊重できていない | `matchMedia('(prefers-reduced-motion: reduce)')` で `visual.js` は `set('off')`、`audio` は `lfo` / `conv` 無効。トグルを `config` にも追加 | **P0 S** |
| A-3 | コントラスト | `#f2e8d0` / `rgba(10,9,7,.94)` は OKだが、`.hub-note rgba(238,228,205,.55)` / `.doc p .78` は WCAG AA (4.5:1) ギリギリ。`#8fb98a` (done) と `#cbb27c` の差は色覚多様性で判別しにくい | 弱視/色覚で可読性低下 | `hint` 系を `.60→.78` に上げる、done は色だけでなく `✔` + 下線 + パターン（既に一部あり）を徹底。`axe` で閾値チェックをCI化 | **P1 S** |
| A-4 | 代替テキスト/ラベル | `bgImg` `cgImg` `titleKey` は `alt=""` 空。ギャラリーの `LOCKED` セルに `aria-disabled` なし | 画像の意味が伝わらない。ロック状態が分からない | `alt` は台帳の `label` を流し込む。`cg-cell.lock` は `aria-disabled="true" title="未回収"` + `tabIndex=-1` | **P1 S** |
| A-5 | HUB/選択肢の選択状態 | `.hub-card` は `hover` だけで `aria-pressed/selected` なし。`#choiceList` の現在選択に `aria-activedescendant` なし | SRが「今どこを選んでいるか」を読まない | choices は `role=listbox` + `role=option aria-selected` + `activedescendant`、HUBは `aria-current` | **P1 S** |
| A-6 | 文字サイズ/行間/追従拡大 | `max-height:32dvh` で長文がスクロールになるが、200%拡大時の検証なし。`ruby` の `<rt>` が 0.5em で潰れる場合 | 拡大ユーザで可読性低下 | `clamp()` と `@media (min-width)` での再テスト、フォント拡大時のオーバーレイ `max-height` を `vh` → `dvh` + `min()` で再調整 | **P2 M** |
| A-7 | キーボード完全操作 | ギャラリー拡大 `cg-view` は `← →` で前後移動できない。`Q` クイックメニューは `1-9` 未対応 | マウス必須の箇所が残る | `cg-view` に `ArrowLeft/Right` と `+ - 0` ズーム、フォーカスリングは `gold` を維持 | **P2 S** |

---

## 2) パフォーマンス / 配信

| # | 項目 | 現状 | 数値 | 提案 |
|---|---|---|---|---|
| P-1 | **CSS 92KB (1156行) を無圧縮で配信**。`will-change` が7セレクタ常時 | FCPは速いが `will-change` 常時はGPUメモリ増。`@import` の Google Fonts がレンダーブロック | **P0 S**: `will-change` は `:hover/.in/.on` の瞬間だけ付与に変更。`cssnano` or 手動minで `-35%`。Fonts は `&display=swap` はOKだが `preload` は `title_key.jpg` のみ — フォントも `preload` + `font-display: swap` 明示 |
| P-2 | **画像配信**: 201スロット中 実画像1枚 (`title_key 238KB`) 以外は 810Bの白PNG。Gitに99枚の `_buffer` (400KB) を含む | `git clone` が1.5MB膨張。将来 52枚のCGをWebPで置く時に差が激しい | **P0 S**: `_buffer` は `.gitignore` or `git lfs`。実画像は `WebP + PNG fallback` + `<picture>` or `image-set()`。`title_key` は `640w/1280w/1920w` の `srcset` と `fetchpriority=high` 済みは維持 |
| P-3 | 起動時 `assets.preload` は並列6だが タイムアウト15s/枚。失敗してもSVGフォールバックで続行はするがリトライなし | 回線細いと `boot` が15s固まる | **P1 S**: `AbortController` + 指数バックオフ1回。`onerror` は `console.warn` だけでなく `bootTip` に「軽量モードで続行」表示 |
| P-4 | `requestAnimationFrame` 粒子は **タブ非表示でも回り続ける** | バックグラウンドでCPU/バッテリー消費 | **P0 S**: `document.visibilitychange` で `Particles.set(null)` → 復帰時 `set(prev)`。`titleFx` も同様 |
| P-5 | `backdropSVG` / `figureSVG` は毎回文字列生成。`warmUp()` で一巡するがキャッシュなし | 同じ `kyoshitsu/hiru` を何度も再計算 | **P2 S**: `Map<string, string>` で `memoize`。HUB往復時の再描画が 2-3ms → 0ms |
| P-6 | JSは **非minify 4ファイル合計 ~110KB** (gzipで ~32KB)。`type=module` のまま waterfall | 初回は `parser.js → state.js → main.js` の順に解決 | **P2 M**: `esbuild` で1バンドル+ `importmap` or `preload module`。`Cache-Control: immutable` とハッシュ付与は配信側で |

**計測（ローカル `python -m http.server`）**: 起動 2.3s（`vncheck`計測と一致）、FCPは手元で 0.9s相当（Fonts込み）。上記P-1〜P-4で 2.3s→1.6s前後まで詰められる見込み。

---

## 3) UX / ゲーム体験

| # | 項目 | 現状 | 提案 |
|---|---|---|---|
| U-1 | **初回チュートリアルなし**。操作は `TIPS/KEYS` にあるが、序章で `Backspace:バックログ / Q:MENU / F1:クイックセーブ` を一度も教えない | 序章 `prologue_001` の地の文に1回だけ吹き出し or 章カード裏に「操作方法」ミニカードを差し込む（スキップ可能） | **P1 S** |
| U-2 | **HUDの `heart` は意図的に非表示**だが、エンドリストでは表示。プレイヤは「今どれくらい寄り添えているか」を推測できない | 年鑑同様、HUBや章カードで **“心の手触り” を曖昧に示す**（例: 3段階の光の粒 “かすか / あたたか / つよい”）。数値は出さないままフィードバックを返す | **P1 M** |
| U-3 | **スキップが「既読/未読を区別しない」**。`Ctrl` 長押しで未読も飛ぶ | `store.meta` に `readScenes` を持ち、`skip` は既読のみ高速化、未読は `×2` に抑えるのが定石。`config` に「未読スキップを許可」トグル | **P1 M** |
| U-4 | **オートの待ち時間が固定** (`autoDelay 1500ms`)。長文でも短文でも同じ | 文字数比例 `max(800, min(4000, 文字数*45 + 600))` に。`text.js` の `PAUSE`（句読点ウェイト）も反映すると自然 | **P1 S** |
| U-5 | **セーブサムネ**: 白PNGのSVG代替は表示されるが、実画像がないと全スロット同じ見た目 | 背景色 `mood.sky` と `cardNo` の文字をサムネに焼き込む（現在は `backdropSVG` をそのまま縮小）。章タイトル1行を入れると判別しやすい | **P2 S** |
| U-6 | **エンド後の導線**: BONUS解放時に `title` に出現するが、エンドカードでは「タイトルへ→BONUS」しかない | エンドカードに **“次におすすめのルート”**（未読の中で `flagcount` が伸びやすい1本）を1件サジェスト。周回モチベが上がる | **P2 S** |
| U-7 | **バックログの検索/フィルタなし** | `LOG` が200件溜まると目的の台詞を探せない。話者フィルタ + `✝本質✝` ハイライト検索を `ovBody` に追加 | **P2 M** |

---

## 4) 堅牢性 / セーブ互換 / エラー耐性

| # | 項目 | 現状 | リスク | 提案 |
|---|---|---|---|---|
| R-1 | **`localStorage` クォータ超過時の沈黙**。`store.js` の `set()` は `catch` で `mem` に逃がすがユーザに通知しない | 12スロット+autoで 5MB を超えると保存したつもりが消える | `QuotaExceededError` を捕捉して `toast('容量不足：古いスロットを1つ削除してください')` + `try { localStorage.setItem } catch` で `confirm` 誘導 | **P0 S** |
| R-2 | **セーブのスキーマバージョニングなし**。`DEFAULT_META` に新キーを足しても旧セーブは `Object.assign` で浅くマージされるだけ | 将来 `data/meta.json` の `flags` を追加した時に旧セーブが `undefined` を持つ | `K_META = honshitsu.meta.v2` 的なマイグレーション関数 `migrate(old) => new` を `Store` に用意。`save()` に `version: 2` を埋める | **P1 S** |
| R-3 | **`judge()` に BONUS分岐がない**。`state.js` は `comedy/true/good/normal/bittersweet` の5分岐のみ。BONUSは `persistent.endings_count>=13` で `shell.js` 側が `title` に生やす | 将来 `judge` を他所で呼んだ時に BONUS が返らない。`vncheck` の「BONUS解禁条件」は `shell` 依存 | `judge()` に `if (Object.keys(persistent.endings).length>=13) return bonus` を明文化 or 少なくともコメントで「BONUSは判定外」を明記。テストで `judge` 単体でも bonus を返すケースを追加 | **P1 S** |
| R-4 | **`evalCond` の文字列比較が `undefined` を `文字列` として扱う**。`getVar` が `undefined` を返すと `bestflag==mie` で `mie` が変数扱いか文字列扱いか曖昧 | スクリプト側のタイポ `besteflag` が気づかれず偽になり続ける | `evalCond` に `unknownVars` を収集して `vncheck` で警告。`strict` モードでは `ReferenceError` を投げるオプション | **P2 S** |
| R-5 | **二重実装 `game/` のドリフト**。直下版と `game/` 版で `ASSET_MAP.md` / `SAVE_KEY` (`honshitsu.save.` vs `honzitsu_save_`) / `judge` ロジックが微妙に違う | どちらを“正”にするか決めないと修正が二重管理 | READMEの「2つの完全実装」表記は残しつつ、**正本を直下版に一本化**し `game/` は `archive/` に退避 or `submodule` 化を明記 | **P2 M** |

---

## 5) セキュリティ / 配信

| # | 項目 | 現状 | 提案 |
|---|---|---|---|
| S-1 | **`innerHTML` が14箇所**。`esc()` はしているがレビューの網が広い。`shell.js:734` の `cgBackdrop` はSVGを生で埋める | `shell.js` の `innerHTML` を `createElement` + `textContent` に置換できる箇所は置換。SVGは `DOMPurify` or `createContextualFragment` + ホワイトリスト。`Trusted Types` を `index.html` のCSPで有効化 | **P1 M** |
| S-2 | **CSPなし**。`index.html` に `<meta http-equiv="Content-Security-Policy">` なし | 将来 OGPやフォントCDNを追加する時にインジェクション余地 | `default-src 'self'; img-src 'self' data: https://fonts.gstatic.com; style-src 'self' https://fonts.googleapis.com; script-src 'self'` を `serve.py` / 本番CDNで付与 | **P1 S** |
| S-3 | **依存の固定なし**。`package.json` がなく `jsdom` は `npm i --no-save` | CIでバージョンがブレる | `package.json` + `package-lock.json` を追加（今回の `jsdom` 検証を `npm ci` で再現可能に） | **P1 S** |

---

## 6) 周辺 / 運用

| # | 項目 | 現状 | 提案 |
|---|---|---|---|
| O-1 | **OGP/Twitter Card なし**。`description` と `theme-color` のみ | SNSでシェアされた時に白いカードになる | `og:title/og:description/og:image` に `title_key.jpg` (1200x630にトリミングした派生) + `og:type=website`。`index.html` に6行追加でCTRが跳ねる | **P0 S** |
| O-2 | **PWA未対応** (`manifest.json` / `service-worker.js` なし)。オフラインで真っ白 | 電車内で読めない。ホーム追加もできない | `manifest.json` (name/short_name/icons/display:standalone) + `sw.js` で `data/script/*.txt` と `assets/*` を `CacheFirst`。初回はオンライン必須、2回目からオフライン可 | **P1 M** |
| O-3 | **CIなし** | `vncheck` / `smoke` は手動。回帰に気づかない | `.github/workflows/ci.yml` で `node tools/vncheck.mjs && npm i --no-save jsdom && node tools/smoke.mjs` を `push/PR` で実行。`playwright` でスクショ差分も | **P1 M** |
| O-4 | **計測なし**。プレイ完走率/離脱シーンが分からない | どこで積むか分からない | 匿名の `beacon` (同意取得後) で `sceneId/start/end` のみ送信。`localStorage` に `optIn` を持つ。無効なら何も飛ばない | **P2 M** |
| O-5 | **フォントのサブセット化なし**。Google Fontsの3ファミリをフルロード | 初回 120KBほど。`Zen Old Mincho` は本文でしか使わない | `&text=` サブセット or `unicode-range` 分割。あるいは `Noto Serif JP` に一本化して `Zen Kaku` をUI専用に | **P2 S** |

---

## 7) アセット/コンテンツ

| # | 項目 | 現状 | 提案 |
|---|---|---|---|
| C-1 | **実写差し替えのガイドが分散** (`README` / `docs/ASSET_MANIFEST.md` / `data/assets.json` の `note`) | 初心者が「どこを `false` にするのか」で迷う | `tools/asset_check.mjs` で「`placeholder:true` だが同名PNGが実在する行」を検出して `toast` で教える。`README` に “差し替え3ステップ” の図を1枚 | **P1 S** |
| C-2 | **未使用BG 1枚 `title_key`** が `vncheck` で毎回警告 | `data/assets.json` で `cat:title` に分離するか `vncheck` の `unused` 判定から除外 | **P2 S** |
| C-3 | 立ち絵の `mix-blend-mode: multiply` 前提で **透過PNGだと暗く沈む** | `config.blend` で `multiply/normal` を切替えられるが、スロット毎の `blend` を持てない | `assets.json` に `blend: multiply|normal` を追加し `Stage` でスロット毎に切替 | **P2 S** |

---

## 8) “やらない”と決めることも改善

- **多言語化**: 現状 `lang: ja` 固定で正解。英訳は台詞のトーンが崩れるため、やるなら `game/` 版を英訳ブランチに分離。
- **WebGL化**: 現状の SVG+Canvas+CSS で十分。WebGLは学習コストに見合わない。
- **動画/BGMの実素材同梱**: 手続き生成のままが軽くて正解。実素材は `audio/bgm01.ogg` を置いた時だけ差し替える `audio.json` 方式が最小侵襲。

---

## ロードマップ案（次の3PR）

**PR-A (Polish P0, 1日)**: A-1, A-2, P-1(一部), P-4, R-1, O-1  
→ キーボードで最後まで遊べる + OS設定尊重 + OGPでシェア映え

**PR-B (Quality P1, 1.5日)**: U-1, U-3, U-4, R-2, R-3, S-1, O-3  
→ 初回体験と周回の手触り、セーブの未来互換、CIの土台

**PR-C (Delight P1-P2, 1日)**: P-2, P-3, C-1, O-2, A-5  
→ 配信最適化 + PWA + アセット体験の親切化

---

## 付記 — 今回の POLISH で“済んだ”こと

- 粒子の奥行き/花びら形状/明滅の自然さ、背景の blur ディップ、立ち絵の呼吸、選択肢/HUBのスタッガー、ヴェールの光掃引、タイトルのパララックス、HUDの `pop`、トーストの sheen — は本PRで対応済み。
- 残りは **「見た目以外の完成度」**。ここを詰めると、素材を差し替えた瞬間に“商業の隣”に立つ。

> 要望があれば、上記ロードマップの **PR-A から即着手**します。どこから切るか教えてください — こちらでブランチを切って進めます。

---

## 更新（同日夕方 / プレイ中の重さ・滑らかさ pass）

実測して直したので、上の表は次のとおり状態が変わった。詳細と数値は **`docs/PERF_2026-09-11.md`**。

| 行 | 状態 | 内容 |
|---|---|---|
| P-1 | **対応済（設計を変えた）** | `will-change` の常時付与は廃止。背景は差し替え中だけ `.bg-slab.fade{will-change:opacity}`、立ち絵は `contain:layout style` に緩め、`infinite` を lite/off で束ねた（CONFIG「立ち絵の動き」）。 |
| P-4 | **対応済** | `Particles` に `visibilitychange` の停止／復帰（`js/visual.js`）。加えてパネル・HUB・端末画面を開いている間はグレイン／スキャン／粒子を `visibility:hidden`、立ち絵アニメを `animation-play-state:paused` にした（`:has()`）。 |
| P-5 | **対応済** | `backdropSVG` は `id|meta` キー、`figureSVG` は `slug|expr` キーでメモ化。さらに **1ラインごとに立ち絵SVGを書き戻していた処理そのものを撤廃**（`_chrDirty` / `artKey`）。 |
| P-2 | **保留（素材作業）** | WebP化と `_buffer` 整理は未着手。ただし実素材化に備えて ①全面 `feTurbulence` をタイル化 ②`contain:paint` × multiply で実写が黒く潰れる不具合を修正 ③優先プリロード（読む5シーンぶんだけ）— まで入れてあるので、差し替えた瞬間に重くなる状態は回避済み。 |
| C-3 | **対応済** | blend を CSS 宣言一本に統一（JS の inline `style.mixBlendMode` 書きを廃止）。既定 `normal`、`multiply` は `#stage[data-blend="multiply"]` のときだけ立ち絵に掛かる。背景／CGは常に `normal`。 |

新規に判った発熱源（表になかったもの）:  **SVG内 `infinite` アニメ＋全面 `feTurbulence`**、
**`#textbox` の `backdrop-filter`**、**1ライン平均1.7KBの localStorage 同期書き込み**（→ 0.08KB/ラインへ）。
ログの量そのものは主犯ではなかった（メモリ内240／スナップショット40／パネル150に短縮はした）。
